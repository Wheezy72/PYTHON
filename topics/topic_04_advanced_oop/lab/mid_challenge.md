# Mid challenge: registry and store

Build the requested SentinelFlow OOP component in the matching solution module. Keep contracts explicit, avoid shared mutable state, and document time and space complexity in comments or README notes.

Requirements:
- Preserve input event data unless the challenge asks for enrichment.
- Use object contracts rather than loose dictionaries where behavior is required.
- Raise clear errors for invalid processors or duplicate configuration.
